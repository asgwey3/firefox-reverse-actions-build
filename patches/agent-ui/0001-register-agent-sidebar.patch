diff --git a/browser/app/profile/firefox.js b/browser/app/profile/firefox.js
index 3ed3716038..03cfaf5819 100644
--- a/browser/app/profile/firefox.js
+++ b/browser/app/profile/firefox.js
@@ -2194,12 +2194,8 @@ pref("pdfjs.previousHandler.alwaysAskBeforeHandling", false);
 pref("pdfjs.handleOctetStream", true);
 
 // Is the sidebar positioned ahead of the content browser
-pref("sidebar.position_start", true);
-#ifdef NIGHTLY_BUILD
+pref("sidebar.position_start", false);
 pref("sidebar.revamp", true);
-#else
-pref("sidebar.revamp", false);
-#endif
 pref("sidebar.revamp.round-content-area", true);
 pref("sidebar.animation.enabled", true);
 pref("sidebar.animation.duration-ms", 200);
@@ -2209,11 +2205,14 @@ pref("sidebar.animation.expand-on-hover.delay-duration-ms", 200);
 // This pref is used to store user customized tools in the sidebar launcher and shouldn't be changed.
 // See https://firefox-source-docs.mozilla.org/browser/components/sidebar/docs/index.html for ways
 // you can introduce a new tool to the sidebar launcher.
-pref("sidebar.main.tools", "");
+pref("sidebar.main.tools", "agent");
+pref("sidebar.newTool.migration.agent", "{}");
 pref("sidebar.installed.extensions", "");
 pref("sidebar.verticalTabs", false);
 pref("sidebar.verticalTabs.dragToPinPromo.dismissed", false);
 pref("sidebar.visibility", "always-show");
+pref("frx.forceHorizontalTabs", true);
+pref("frx.hideRemoteControlCue", true);
 // Sidebar UI state is stored per-window via session restore. Use this pref
 // as a backup to restore the sidebar UI state when a user has PPB mode on
 // or has history cleared on browser close.
@@ -2227,7 +2226,7 @@ pref("sidebar.openTabsPanel.enabled", false);
 
 pref("sidebar.notification.badge.aichat", false);
 
-pref("browser.ml.chat.enabled", true);
+pref("browser.ml.chat.enabled", false);
 pref("browser.ml.chat.hideLocalhost", true);
 pref("browser.ml.chat.maxLength", 7000);
 pref("browser.ml.chat.menu", true);
diff --git a/browser/base/content/browser.js b/browser/base/content/browser.js
index 45ed2c9dd0..584fbcfdd3 100644
--- a/browser/base/content/browser.js
+++ b/browser/base/content/browser.js
@@ -3919,6 +3919,14 @@ const gRemoteControl = {
   },
 
   updateVisualCue() {
+    const hideFrxRemoteControlCue =
+      Services.prefs.getBoolPref("frx.hideRemoteControlCue", false) ||
+      Services.env.get("MOZ_FRX_HIDE_REMOTE_CONTROL_CUE") === "1";
+    if (hideFrxRemoteControlCue) {
+      document.documentElement.removeAttribute("remotecontrol");
+      return;
+    }
+
     // Disable updating the remote control cue for performance tests,
     // because these could fail due to an early initialization of Marionette.
     const disableRemoteControlCue = Services.prefs.getBoolPref(
diff --git a/browser/components/sidebar/SidebarManager.sys.mjs b/browser/components/sidebar/SidebarManager.sys.mjs
index 3d1b64ba6a..e7103a3225 100644
--- a/browser/components/sidebar/SidebarManager.sys.mjs
+++ b/browser/components/sidebar/SidebarManager.sys.mjs
@@ -15,7 +15,7 @@ const PINNED_PROMO_PREF = "sidebar.verticalTabs.dragToPinPromo.dismissed";
 // New panels that are ready to be introduced to new sidebar users should be added to this list;
 // ensure your feature flag is enabled at the same time you do this and that its the same value as
 // what you added to .
-const DEFAULT_LAUNCHER_TOOLS = "aichat,syncedtabs,history,bookmarks,opentabs";
+const DEFAULT_LAUNCHER_TOOLS = "agent";
 const lazy = {};
 ChromeUtils.defineESModuleGetters(lazy, {
   BrowserWindowTracker: "resource:///modules/BrowserWindowTracker.sys.mjs",
diff --git a/browser/components/sidebar/browser-sidebar.js b/browser/components/sidebar/browser-sidebar.js
index 36f7e1ceba..4fb99e63d1 100644
--- a/browser/components/sidebar/browser-sidebar.js
+++ b/browser/components/sidebar/browser-sidebar.js
@@ -14,6 +14,7 @@ const { DeferredTask } = ChromeUtils.importESModule(
 
 const toolsNameMap = {
   viewGenaiChatSidebar: "aichat",
+  viewAgentSidebar: "agent",
   viewGenaiPageAssistSidebar: "aipageassist",
   viewTabsSidebar: "syncedtabs",
   viewHistorySidebar: "history",
@@ -420,6 +421,13 @@ var SidebarController = {
   },
 
   init() {
+    if (
+      Services.prefs.getBoolPref("frx.forceHorizontalTabs", true) &&
+      Services.prefs.getBoolPref("sidebar.verticalTabs", false)
+    ) {
+      Services.prefs.setBoolPref("sidebar.verticalTabs", false);
+    }
+
     // Initialize global state manager.
     this.SidebarManager;
 
@@ -710,6 +718,9 @@ var SidebarController = {
    * Toggle the vertical tabs preference.
    */
   toggleVerticalTabs() {
+    if (Services.prefs.getBoolPref("frx.forceHorizontalTabs", true)) {
+      return;
+    }
     Services.prefs.setBoolPref(
       "sidebar.verticalTabs",
       !this.sidebarVerticalTabsEnabled
diff --git a/browser/components/sidebar/sidebar-customize.mjs b/browser/components/sidebar/sidebar-customize.mjs
index 60c31f4c04..f9d2953952 100644
--- a/browser/components/sidebar/sidebar-customize.mjs
+++ b/browser/components/sidebar/sidebar-customize.mjs
@@ -17,6 +17,7 @@ const { XPCOMUtils } = ChromeUtils.importESModule(
 const l10nMap = new Map([
   ["viewGenaiChatSidebar", "sidebar-menu-genai-chat-label"],
   ["viewGenaiPageAssistSidebar", "sidebar-menu-genai-page-assist-label"],
+  ["viewAgentSidebar", "sidebar-menu-agent-label"],
   ["viewHistorySidebar", "sidebar-menu-history-label"],
   ["viewTabsSidebar", "sidebar-menu-synced-tabs-label"],
   ["viewBookmarksSidebar", "sidebar-menu-bookmarks-label"],
